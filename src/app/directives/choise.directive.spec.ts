import { ChoiseDirective } from './choise.directive';

describe('ChoiseDirective', () => {
  it('should create an instance', () => {
    const directive = new ChoiseDirective();
    expect(directive).toBeTruthy();
  });
});
